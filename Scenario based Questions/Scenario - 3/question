Scenario: A data team requires a MySQL database with persistent storage for analytics.

Question: Deploy a MySQL database (mysql:8) with a StatefulSet, a Secret for credentials, and a 1Gi PVC. Verify data persistence after a Pod restart.